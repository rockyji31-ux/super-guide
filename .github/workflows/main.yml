name: RDP Auto

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Enable RDP & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall set rule group="Remote Desktop" new enable=Yes

      - name: Create User
        run: |
          $pass = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 16 | % {[char]$_})
          $securePass = ConvertTo-SecureString $pass -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_PASSWORD=$pass" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "$env:TEMP\tailscale-setup.exe"
          Start-Process "$env:TEMP\tailscale-setup.exe" -ArgumentList "/quiet", "/norestart" -Wait

      - name: Connect Tailscale
        run: |
          # Secret Key use kar rahe hain
          $Key = "${{ secrets.TAILSCALE_AUTHKEY }}"
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$Key --hostname="gh-runner-$env:GITHUB_RUN_ID" --accept-routes --unattended
          
          $tsIP = $null
          for ($i=1; $i -le 24; $i++) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              if ($tsIP) { break }
              Start-Sleep -Seconds 5
          }
          if (-not $tsIP) { exit 1 }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Show Details in GitHub & Send to n8n
        run: |
          # 1. GitHub Console me Show karna (Aapki Demand)
          Write-Host " "
          Write-Host "============================================="
          Write-Host "       🖥️ RDP CREATED SUCCESSFULLY           "
          Write-Host "============================================="
          Write-Host "🌐 IP:       $env:TAILSCALE_IP"
          Write-Host "👤 User:     RDP"
          Write-Host "🔑 Pass:     $env:RDP_PASSWORD"
          Write-Host "============================================="
          Write-Host " "

          # 2. n8n ko bhejna
          $WebhookUrl = "https://65.1.91.110.nip.io/webhook-test/rdp-credentials"
          $body = @{
              user = "RDP"
              password = $env:RDP_PASSWORD
              ip = $env:TAILSCALE_IP
          } | ConvertTo-Json
          
          try {
            Invoke-RestMethod -Uri $WebhookUrl -Method Post -Body $body -ContentType "application/json"
          } catch {
            Write-Warning "Webhook failed but RDP is running."
          }

      - name: Keep Alive
        run: |
          while ($true) { Start-Sleep -Seconds 300 }
